@model IEnumerable<Task5.WEB.Models.ManagerViewModel>

@{
    ViewBag.Title = "Managers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm("Index", "Manager", FormMethod.Get))
{
    <p>
        Filter by last name: @Html.TextBox("searchString")
        <input type="submit" value="Search" />
    </p>
}

<table class="table">
    <tr>
        <th>
            Last name
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lastname)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditManager", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Delete", "DeleteManager", new { id = item.Id })
            </td>
        </tr>
    }

</table>
